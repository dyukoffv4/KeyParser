#pragma once

#include <string>
#include <vector>
#include <exception>
#include <stdexcept>

namespace keyparser {
    class Key;
    class Parser;
    class Task;

    typedef std::vector<std::string> Args;
}